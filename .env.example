# Brevo Transactional Email API (server-side only)
# Get an API key from https://app.brevo.com/settings/keys/api (SMTP & API key)
# Never prefix these with NEXT_PUBLIC_ and never commit real values.
BREVO_API_KEY=
CONTACT_EMAIL=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=